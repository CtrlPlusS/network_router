# 1. 빌드 디렉토리 (가장 중요)
build/

# 2. 실행 파일 (Binary)

# CMakeLists.txt에 의해 생성되는 메인 실행 파일
router_engine

# playground의 연습용 폴더와 실행 파일들 (접두사 practice_로 시작)
practice/
practice_*

# 혹시 모를 기본 출력 파일
a.out

# 3. IDE 및 에디터 설정
.vscode/
.idea/
.cache/
*.swp
*~

# 4. OS 및 시스템 자동 생성 파일
.DS_Store
Thumbs.db

# 5. CMake 임시 파일 (혹시 build 폴더가 아닌 루트에서 cmake를 실행했을 경우 대비)
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile

#6. 로그 파일
log/
*.log